import{Search}from'lucide-react';import{useState}from'react';
export default function SearchBar({onSearch,loading}){const[topic,setTopic]=useState(''),[amount,setAmount]=useState(5);const go=e=>{e.preventDefault();if(topic.trim())onSearch(topic,amount)};return <form onSubmit={go} className="mx-auto max-w-3xl"><div className="flex items-center gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-soft focus-within:border-brand-500 dark:border-slate-700 dark:bg-slate-900"><Search className="ml-3 text-slate-400"/><input autoFocus className="min-w-0 flex-1 bg-transparent px-2 py-3 text-lg" placeholder="Digite um assunto..." value={topic} onChange={e=>setTopic(e.target.value)}/><select aria-label="Quantidade" className="rounded-lg bg-slate-100 px-2 py-3 text-sm dark:bg-slate-800" value={amount} onChange={e=>setAmount(+e.target.value)}><option>5</option><option>10</option><option>20</option></select><button disabled={loading||!topic.trim()} className="btn-primary px-5">Gerar</button></div><p className="mt-4 text-center text-sm text-slate-500">Experimente: Docker · Java · Spring Security · RabbitMQ · Crase</p></form>}
